[Unit]
Description=todo-list-mcp frontend service
After=network.target

[Service]
Type=simple
User=deploy
WorkingDirectory=/opt/todo-list-mcp/current/client
ExecStart=/usr/bin/npm run preview:prod
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
